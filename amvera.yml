version: null
meta:
  environment: node
  toolchain:
    name: npm
    version: "18"
build:
  skip: false
  additionalCommands: npm --prefix app install && npm --prefix app run build
  artifacts:
    "*": /
run:
  scriptName: server.js
  scriptArguments: null
  nodeArguments: null
  command: npm run start
  persistenceMount: /data
  containerPort: 3000
