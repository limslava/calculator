# 📚 Полная документация системы расчета логистических ставок

## 1. Введение

### 1.1. О проекте
Система предназначена для автоматизации расчета стоимости логистических перевозок с поддержкой различных типов транспорта:
- **Морские перевозки** (прямые и комплексные)
- **Железнодорожные перевозки** (прямые и комплексные)
- **Комплексные перевозки** (море + железная дорога)

### 1.2. Роли пользователей
- **Администратор** – полный доступ ко всем функциям, управление пользователями, настройка системы.
- **Менеджер по закупкам** – загрузка и обновление ставок, управление тарифами, работа с базами данных.
- **Менеджер по продажам** – расчет стоимости перевозок, поиск оптимальных маршрутов, работа с клиентами.

### 1.3. Быстрый старт
**Предварительные требования:**
- Node.js 16+
- PostgreSQL 12+
- Созданная база данных PostgreSQL

**Настройка базы данных:**
1. Создайте базу данных PostgreSQL.
2. Настройте переменные окружения в файле `.env`:
   ```bash
   DATABASE_URL=postgresql://username:password@localhost:5432/database_name
   JWT_SECRET=your-secret-key-change-in-production
   ```

**Установка зависимостей:**
```bash
npm install
```

**Запуск сервера:**
```bash
npm start
```

**Доступ к приложению:**  
Откройте в браузере: `http://localhost:3000`

**Данные для входа по умолчанию:**
- Логин: `admin@logistics.com`
- Пароль: `admin123`

---

## 2. Структура проекта

```
Khasan/
├── 📄 index.html              # Главная страница
├── 📄 script.js               # Основной скрипт приложения
├── 📄 server.js               # Сервер Node.js с PostgreSQL
├── 📄 server-data.js          # Хранилище данных с поддержкой базы данных
├── 📄 styles.css              # Стили приложения
├── 📁 interfaces/             # HTML интерфейсы
│   ├── purchaser-interface.html
│   ├── sales-interface.html
│   └── reset-admin.html
├── 📁 scripts/                # JavaScript файлы
│   ├── purchaser-script.js
│   ├── sales-script.js
│   ├── auth.js
│   ├── auth-ui.js
│   ├── utils.js
│   ├── reset-admin.js
│   └── server-auth.js         # Серверная аутентификация
├── 📁 modules/                # Модули для работы с данными
│   ├── sea.js
│   ├── rail.js
│   ├── direct-sea.js
│   └── direct-rail.js
├── 📁 config/                 # Конфигурационные файлы
│   ├── database.js            # Конфигурация PostgreSQL
│   ├── auth.js                # Настройки аутентификации
│   └── email-config.js        # Настройки email
├── 📁 models/                 # Модели Sequelize для базы данных
│   └── index.js
├── 📁 docs/                   # Документация
│   ├── DEPLOYMENT_GUIDE.md    # Полное руководство по развертыванию
│   ├── ADMIN_GUIDE.md         # Руководство по администрированию
│   └── TECHNICAL_DOCUMENTATION.md # Техническая документация
├── 📁 templates/              # Шаблоны Excel файлов
│   ├── template-sea.xlsx
│   ├── template-rail.xlsx
│   ├── template-direct-sea.xlsx
│   └── template-direct-rail.xlsx
├── 📁 tests/                  # Тестовые файлы
│   ├── test-fixed-redirect.html
│   └── test-redirect.html
├── 📁 .github/workflows/      # CI/CD конфигурации
└── 📄 package.json            # Зависимости проекта
```

---

## 3. Настройка базы данных

### 3.1. Локальная установка PostgreSQL
```bash
# Установка PostgreSQL (macOS)
brew install postgresql
brew services start postgresql

# Создание базы данных
createdb logistics_calculator
```

### 3.2. Облачная база данных (рекомендуется для продакшена)
Используйте любого облачного провайдера:
- PostgreSQL на Amvera
- Amazon RDS
- Google Cloud SQL
- Azure Database for PostgreSQL
- Heroku PostgreSQL

### 3.3. Переменные окружения
Создайте файл `.env` в корне проекта:
```env
# База данных
DATABASE_URL=postgresql://username:password@localhost:5432/logistics_calculator

# JWT секрет
JWT_SECRET=your-very-secure-jwt-secret-key

# Окружение
NODE_ENV=development
```

### 3.4. Инициализация базы данных
При запуске сервера база данных автоматически инициализируется:
```bash
npm start
```
Это создаст:
- Таблицы базы данных
- Администратора по умолчанию (admin@logistics.com / admin123)
- Начальные данные для всех типов логистики
- Пустые наборы данных для морских, железнодорожных и прямых перевозок
- Начальные тарифные данные

### 3.5. Структура базы данных
**Таблицы:**
1. **users** – пользователи системы (id, email, password, role, isActive, lastLogin, createdAt, updatedAt)
2. **sea_data** – данные морских перевозок (id, data (JSONB), lastUpdate, count)
3. **rail_data** – данные железнодорожных перевозок (id, data (JSONB), lastUpdate, count)
4. **direct_rail_data** – данные прямых железнодорожных перевозок (id, data (JSONB), lastUpdate, count)
5. **direct_sea_data** – данные прямых морских перевозок (id, data (JSONB), lastUpdate, count)
6. **tariff_data** – тарифные данные (id, data (JSONB), lastUpdate, count)

---

## 4. Развертывание на Amvera

### 4.1. Настройка переменных окружения в Amvera
**Обязательные переменные:**
```
DATABASE_URL=postgresql://amvera-limslava-cnpg-postgresql-calcul-rw
NODE_ENV=production
PORT=3000
JWT_SECRET=your-very-secure-jwt-secret-key-here
```

**Рекомендуемые переменные:**
```
LOG_LEVEL=info
CORS_ORIGIN=*
```

### 4.2. Конфигурация базы данных
Приложение автоматически:
- Создает необходимые таблицы при первом запуске
- Инициализирует администратора (логин: `admin`, пароль: `admin123`)
- Загружает начальные данные

### 4.3. Процесс развертывания
1. **Подготовка репозитория:**
   ```bash
   git add .
   git commit -m "Подготовка к развертыванию"
   git push origin main
   ```
2. **Настройка Amvera:**
   - Войдите в [Amvera Cloud](https://cloud.amvera.ru)
   - Перейдите в проект `calculator-limslava`
   - Убедитесь, что конфигурация загружена из [`amvera.yml`](amvera.yml:1)
3. **Настройка SMTP** (для отправки email):
   - После развертывания откройте `https://calculator-limslava.amvera.io/`
   - Войдите как администратор
   - Перейдите в "Настройки Email" в панели администратора
   - Введите настройки SMTP и протестируйте отправку
4. **Настройка базы данных:**
   - Создайте базу данных PostgreSQL
   - Настройте переменные окружения в Amvera Console
5. **Создание пользователей:**
   - В панели администратора нажмите "Добавить пользователя"
   - Введите email и выберите роль
   - Система автоматически сгенерирует пароль и отправит данные для входа на email

### 4.4. Мониторинг после развертывания
После развертывания проверьте:
1. Логи приложения на наличие ошибок подключения к базе данных
2. Доступность приложения по URL
3. Функциональность авторизации
4. Загрузку данных в интерфейсе продаж

### 4.5. Устранение неполадок
- **Ошибка подключения к базе данных:** Проверьте корректность DATABASE_URL, убедитесь, что база данных доступна из сети Amvera, проверьте настройки SSL.
- **Ошибка авторизации:** Проверьте JWT_SECRET переменную, убедитесь, что она достаточно длинная и безопасная.
- **Предупреждение "Тип данных complex не загружается":** Это нормальное поведение – комплексные расчеты динамически вычисляются и не хранятся в базе данных.

---

## 5. Руководство администратора

### 5.1. Восстановление администратора
Если учетная запись администратора `admin@logistics.com` стала определяться как менеджер по продажам (sales) вместо администратора:

**Способ 1: Использование страницы сброса (рекомендуется)**
1. Откройте файл [`reset-admin.html`](reset-admin.html) в браузере
2. Нажмите кнопку "Сбросить администратора"
3. После успешного сброса перейдите к основному приложению [`index.html`](index.html)
4. Войдите с данными:
   - **Логин**: `admin@logistics.com`
   - **Пароль**: `admin123`
   - **Роль**: Администратор

**Способ 2: Ручной сброс через консоль браузера**
1. Откройте основное приложение [`index.html`](index.html)
2. Откройте консоль разработчика (F12 → Console)
3. Выполните команды (см. [ADMIN_GUIDE.md](docs/archive/ADMIN_GUIDE.md) для деталей)

### 5.2. Исправление проблемы перенаправления из панели администратора
Кнопки "Закупщик" и "Менеджер по продажам" в панели администратора теперь открывают интерфейсы в текущей вкладке (не в новой). При переходе администратора в другие интерфейсы его роль не изменяется, и при выходе система корректно возвращает на главную страницу.

---
## 6. Техническая документация

### 6.1. Архитектура приложения
Приложение представляет собой SPA (Single Page Application) на Node.js с клиентской частью на чистом JavaScript и серверной частью на PostgreSQL.

**Серверная часть:**
- Node.js + Express – сервер для обслуживания статических файлов и API
- PostgreSQL + Sequelize ORM – реляционная база данных для хранения всех данных
- JWT аутентификация – безопасная система авторизации
- Helmet – настройки безопасности заголовков
- Compression – сжатие данных Gzip
- Content Security Policy – политика безопасности контента

**Клиентская часть:**
- HTML/CSS/JavaScript – чистый фронтенд без фреймворков
- SheetJS (XLSX) – обработка Excel файлов
- localStorage – хранение сессий и резервных копий
- Fetch API – взаимодействие с серверными API
- JWT токены – авторизованные запросы к серверу

### 6.2. Типы перевозок
1. **Море** – морские контейнерные перевозки
2. **ЖД** – железнодорожные перевозки
3. **Прямое ЖД** – прямые железнодорожные перевозки
4. **Прямое море** – прямые морские перевозки
5. **Море+ЖД** – комбинированные перевозки (комплексные расчеты)

### 6.3. Формат данных
- **Excel файлы** с определенной структурой столбцов
- **Автоматический поиск** по параметрам перевозки
- **Расчет ставок** на основе загруженных данных

### 6.4. Конфигурационные файлы
**Amvera ([`amvera.yml`](amvera.yml:1)):**
```yaml
name: logistics-calculator
services:
  web:
    build: .
    ports:
      - 3000:3001    # Внешний порт:3000 → внутренний порт:3001
    environment:
      - NODE_ENV=production
      - PORT=3001
    resources:
      memory: 512Mi
      cpu: 500m
```

**Docker ([`Dockerfile`](Dockerfile:1)):**
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
USER nextjs
EXPOSE 3001
CMD ["node", "server.js"]
```

### 6.5. API интеграции
**Курс ЦБ РФ:**
Приложение автоматически получает курс USD через публичное API:
```javascript
async function getExchangeRate() {
    try {
        const response = await fetch('https://www.cbr-xml-daily.ru/daily_json.js');
        const data = await response.json();
        return data.Valute.USD.Value;
    } catch (error) {
        console.error('Ошибка получения курса:', error);
        return 90; // Значение по умолчанию
    }
}
```

### 6.6. Безопасность
**Content Security Policy:**
```javascript
contentSecurityPolicy: {
    directives: {
        defaultSrc: ["'self'"],
        scriptSrc: ["'self'", "'unsafe-inline'"],
        styleSrc: ["'self'", "'unsafe-inline'"],
        imgSrc: ["'self'", "data:", "https:"],
        connectSrc: ["'self'", "https://www.cbr-xml-daily.ru"]
    }
}
```

---

## 7. Архитектура комплексных расчетов

### 7.1. Объяснение сообщения в логах
Сообщение в логах сервера:
```
⚠️ Тип данных "complex" не загружается из базы данных (комплексный расчет)
```
**Это нормальное поведение системы, а не ошибка!**

### 7.2. Почему это происходит
Система использует гибридный подход к хранению данных:

**Типы данных, хранящиеся в PostgreSQL:**
- `sea` – морские перевозки
- `rail` – железнодорожные перевозки
- `direct_rail` – прямые железнодорожные перевозки
- `direct_sea` – прямые морские перевозки
- `tariff` – тарифы и дополнительные услуги

**Тип данных, НЕ хранящийся в PostgreSQL:**
- `complex` – комплексные расчеты (море + железная дорога)

### 7.3. Как работают комплексные расчеты
Комплексные расчеты выполняются динамически на основе данных из других типов:
1. **Сбор данных** из таблиц `sea`, `rail`, `direct_sea`, `direct_rail`
2. **Автоматическое соединение** маршрутов по городам и портам
3. **Конвертация валют** – морские ставки в USD конвертируются в RUB
4. **Сложение ставок** – морская часть + железнодорожная часть
5. **Добавление ВТТ** – при включении триггера ВТТ

### 7.4. Преимущества такого подхода
- **Экономия места в базе данных** – комплексные ставки не дублируют данные
- **Гибкость соединений** – автоматическое нахождение всех возможных комбинаций
- **Актуальность данных** – изменения в отдельных ставках автоматически отражаются в комплексных расчетах
- **Производительность** – отсутствие необходимости в сложных JOIN операциях

### 7.5. API обработка
- **Запрос**: `GET /api/data/complex`
- **Ответ**: `{ data: [], lastUpdate: null, count: 0 }`
- **Сообщение**: Информационное предупреждение в логах

### 7.6. Клиентская обработка
- **Функция**: `Utils.showLastUpdate('complex', 'last-update')`
- **Действие**: Показывает текст "Комплексный расчет (данные не сохраняются)"
- **Расчет**: Выполняется динамически в функции `calculateAllRates()`

---

## 8. План улучшений

### 8.1. Техническое задание на улучшения
Система имеет потенциал для дальнейшего развития. Основные направления улучшений:

**Архитектурные улучшения:**
- Модуляризация кода (разделение на сервисы, компоненты, утилиты)
- Внедрение паттерна Repository для работы с данными
- Создание сервисов для бизнес-логики расчетов
- Разделение UI-логики от бизнес-логики

**Новые возможности:**
- Клиентская валидация форм
- Прогресс-бары для загрузки файлов
- Кеширование результатов расчетов
- Экспорт результатов в PDF/Excel
- История расчетов для пользователей
- API для интеграции с внешними системами

**UX/UI улучшения:**
- Адаптивный дизайн для мобильных устройств
- Темная тема интерфейса
- Уведомления в реальном времени
- Drag & drop для загрузки файлов
- Поиск и фильтрация результатов

### 8.2. Текущий статус улучшений (по состоянию на 2026-01-12)
**Выполнено:**
- ✅ Миграция с JSON файлов на PostgreSQL
- ✅ Создание моделей данных и репозиториев (Sequelize)
- ✅ Базовые меры безопасности (helmet, compression)
- ✅ Docker-контейнеризация
- ✅ CI/CD пайплайн для деплоя на Amvera
- ✅ Тарифы для агентов - добавлена поддержка загрузки тарифов агентов (сверхнормативного пользования) из Excel файлов
- ✅ Исправление интерфейса - удалены лишние элементы, исправлено отображение дат, улучшен UX
- ✅ UI улучшения загрузки файлов - добавлено отображение имени файла, визуальная обратная связь, унифицированные стили
- ✅ Исправление отображения загруженных ставок - всегда показываются последние загруженные ставки, а не история загрузок
- ✅ Исправление модального окна маржинальности - модальное окно теперь открывается только в комплексных ставках, а не в раздельных

**Требует доработки:**
- ❌ Автоматическое тестирование (unit, integration, E2E)
- ❌ TypeScript для типизации
- ❌ Современный фронтенд (React + state management)
- ❌ Кеширование и пагинация
- ❌ Документация API (Swagger) и rate limiting

### 8.3. Детализация UI улучшений (январь 2026)

**Улучшения интерфейса загрузки файлов:**
1. **Визуальная обратная связь при выборе файла:**
   - Добавлено отображение имени выбранного файла и его размера
   - Реализована визуальная индикация через CSS-класс `.file-selected`
   - Унифицированы стили загрузки файлов для всех типов (море/жд/агенты)

2. **Исправление отображения информации о загруженных ставках:**
   - Убраны временные сообщения о выборе файла из зеленого блока статуса
   - Сохранено отображение информации о загруженных ставках в статусе
   - Исправлена логика отображения в блоке "Последние загруженные ставки"

3. **Корректировка логики отображения ставок:**
   - При входе в интерфейс загрузки файлов всегда отображаются последние загруженные ставки для выбранного типа перевозок
   - После загрузки данных в базу также обновляются последние ставки, а не история загрузок
   - Обеспечена консистентность отображения данных

**Технические изменения:**
- В файле [`scripts/purchaser-script.js`](scripts/purchaser-script.js) обновлена функция `uploadDataToDatabase()` для загрузки последних ставок вместо истории
- В файле [`scripts/uploaded-rates.js`](scripts/uploaded-rates.js) исправлена функция `initUploadedRates()` для корректной инициализации отображения ставок
- Убраны вызовы `Utils.showStatus()` при выборе файла, сохранена только визуальная обратная связь через отображение имени файла

### 8.4. Исправление модального окна маржинальности (январь 2026)

**Проблема:** В панели менеджера по продажам в раздельных ставках при нажатии на ставку появлялось модальное окно маржинальности, хотя оно должно показываться только в комплексных ставках.

**Решение:** Убраны атрибуты `ondblclick="openMarginModal(${index})"` из функций отображения раздельных ставок и оставлены только в функции отображения комплексных ставок.

**Технические изменения:**

1. **Функция `displaySeaRates()`** ([`scripts/sales-script.js:804`](scripts/sales-script.js:804)):
   - Было: `<tr ondblclick="openMarginModal(${index})" style="cursor: pointer;">`
   - Стало: `<tr style="cursor: pointer;">`

2. **Функция `displayRailResults()`** ([`scripts/sales-script.js:903`](scripts/sales-script.js:903)):
   - Было: `<tr ondblclick="openMarginModal(${index})" style="cursor: pointer;">`
   - Стало: `<tr style="cursor: pointer;">`

3. **Функция `displayDirectRailRates()`** ([`scripts/sales-script.js:960`](scripts/sales-script.js:960)):
   - Было: `<tr ondblclick="openMarginModal(${index})" style="cursor: pointer;">`
   - Стало: `<tr style="cursor: pointer;">`

4. **Функция `displayDirectSeaRates()`** ([`scripts/sales-script.js:1046`](scripts/sales-script.js:1046)):
   - Было: `<tr ondblclick="openMarginModal(${index})" style="cursor: pointer;">`
   - Стало: `<tr style="cursor: pointer;">`

5. **Функция `displayComplexResults()`** ([`scripts/sales-script.js:1725`](scripts/sales-script.js:1725)):
   - Осталось без изменений: `<tr class="${bestRateClass}" ondblclick="openMarginModal(${index})" style="cursor: pointer;">`

**Результат:** Теперь модальное окно маржинальности открывается только при двойном клике на ставку в комплексных ставках и не открывается в раздельных ставках, что соответствует требованиям системы.

### 8.5. Улучшения раздельных ставок в панели менеджера по продажам (январь 2026)

**Задача:** Работа с раздельными ставками в панели менеджера по продажам.

**Выполненные изменения:**

#### 1. Прямое море
- **Добавлен столбец Remark** в отображение ставок для прямого моря
- **Техническая реализация:** В функции `displayDirectSeaRates()` в файле [`scripts/sales-script.js`](scripts/sales-script.js:1043) добавлен столбец "Remark" в таблицу результатов
- **Источник данных:** Поле `remarks` из данных прямого моря (парсинг из Excel файлов через функцию `parseDirectSeaData()` в [`scripts/utils.js`](scripts/utils.js:561))

#### 2. Прямое ЖД
- **Добавлен столбец "Погран переход"** сразу после города прибытия
- **Реализована сортировка по возрастанию** (столбец ставка)
- **Добавлена подсветка 1 ставки** как в комплексных ставках
- **Техническая реализация:** В функции `displayDirectRailRates()` в файле [`scripts/sales-script.js`](scripts/sales-script.js:953-978):
  - Добавлен столбец "Погран переход" с данными из поля `borderCrossing`
  - Реализована сортировка по ставкам FOB от меньшего к большему
  - Добавлен класс `best-rate-row` для первой строки (самой выгодной ставки)
- **Источник данных:** Поле `borderCrossing` из данных прямого ЖД (парсинг из Excel файлов через функцию `parseDirectRailData()` в [`scripts/utils.js`](scripts/utils.js:469))

#### 3. Подсветка лучшей ставки для прямого моря
- **Добавлена подсветка лучшей ставки** для прямого моря, аналогично прямому ЖД
- **Техническая реализация:** В функции `displayDirectSeaRates()` добавлена логика определения лучшей ставки и применение класса `best-rate-row` для первой строки после сортировки

#### 4. Адаптация изменений для интерфейса продаж
- **Проблема:** Изменения в модулях `direct-sea.js` и `direct-rail.js` не автоматически применялись к интерфейсу продаж, так как там используются собственные функции отображения
- **Решение:** Модифицированы функции в `sales-script.js` аналогично изменениям в модулях
- **Исправление опечатки:** В функции `displayDirectSeaRates()` исправлено использование `result.remark` на `result.remarks` (множественное число) для корректного отображения данных

#### 5. Проверка наличия полей данных
- **Поле `remarks`:** Проверено наличие в данных `direct_sea` через функцию парсинга в `utils.js`
- **Поле `borderCrossing`:** Проверено наличие в данных `direct_rail` через функцию парсинга в `utils.js`
- **Результат:** Оба поля присутствуют в соответствующих шаблонах Excel и корректно парсятся

**Технические файлы, затронутые изменениями:**
- [`scripts/sales-script.js`](scripts/sales-script.js) – основные функции отображения ставок
- [`modules/direct-sea.js`](modules/direct-sea.js) – модуль прямого моря (обновлен ранее)
- [`modules/direct-rail.js`](modules/direct-rail.js) – модуль прямого ЖД (обновлен ранее)
- [`scripts/utils.js`](scripts/utils.js) – функции парсинга Excel данных
- [`styles.css`](styles.css) – стили для класса `best-rate-row`

**Результат:** Интерфейс менеджера по продажам теперь включает все требуемые изменения для раздельных ставок, обеспечивая полную функциональность аналогично интерфейсу менеджера по закупкам.

### 8.6. Рекомендации по приоритетам
1. **Приоритет 1**: Внедрить базовое тестирование (Jest для backend, Cypress для E2E).
2. **Приоритет 2**: Добавить TypeScript и ESLint для улучшения качества кода.
3. **Приоритет 3**: Рассмотреть постепенную миграцию фронтенда на React (можно начать с отдельных компонентов).
4. **Приоритет 4**: Реализовать кеширование и пагинацию для улучшения производительности.
5. **Приоритет 5**: Создать документацию API (Swagger) и добавить rate limiting.

---

## 9. Дополнительные материалы

### 10.1. Конфигурационные файлы
- **`amvera.yml`** – конфигурация для развертывания на Amvera (определяет сервисы, порты, ресурсы).
- **`Dockerfile`** – инструкции для сборки Docker-образа приложения.
- **`.env.production`** – пример переменных окружения для production-окружения.
- **`.dockerignore`** – исключения для Docker-сборки.
- **`.gitignore`** – исключения для Git.

### 10.2. Шаблоны Excel файлов
В папке `templates/` находятся шаблоны для загрузки данных:
- `template-sea.xlsx` – для морских перевозок.
- `template-rail.xlsx` – для железнодорожных перевозок.
- `template-direct-sea.xlsx` – для прямых морских перевозок.
- `template-direct-rail.xlsx` – для прямых железнодорожных перевозок.

Каждый шаблон содержит предопределенные столбцы, необходимые для корректного парсинга системой.

### 10.3. Тестовые файлы
В папке `tests/` и корне проекта находятся HTML-файлы для тестирования отдельных функций:
- `test-auth-fix.html` – тест исправлений аутентификации.
- `test-complex-rates.html` – тест комплексных расчетов.
- `test-update-display.html` – тест отображения обновлений данных.
- `test-fixed-redirect.html` – тест перенаправлений между интерфейсами.

### 10.4. CI/CD конфигурации
В папке `.github/workflows/` расположены файлы GitHub Actions для автоматического развертывания:
- `deploy-amvera.yml` – автоматический деплой на Amvera при пуше в ветку `main`.

### 10.5. Скрипты и модули
- **`scripts/`** – клиентские JavaScript файлы для каждого интерфейса и утилит.
- **`modules/`** – модули расчета ставок (морские, железнодорожные, прямые).
- **`config/`** – конфигурационные файлы для базы данных, аутентификации и email.
- **`models/`** – модели Sequelize для PostgreSQL.

### 10.6. Интерфейсы
- **`interfaces/sales-interface.html`** – интерфейс менеджера по продажам.
- **`interfaces/purchaser-interface.html`** – интерфейс менеджера по закупкам.
- **`interfaces/reset-admin.html`** – страница сброса администратора.

### 10.7. Основные скрипты
- **`server.js`** – основной сервер Node.js.
- **`server-data.js`** – модуль работы с данными (API для PostgreSQL).
- **`script.js`** – главный клиентский скрипт (авторизация, навигация).
- **`styles.css`** – стили приложения.

### 10.8. Полезные команды
```bash
# Инициализация базы данных
npm run db:init

# Сброс базы данных (осторожно!)
npm run db:reset

# Запуск в режиме разработки
npm run dev

# Запуск production-сервера
npm start
```

### 10.9. Ссылки
- **Основной URL приложения:** `https://calculator-limslava.amvera.io/`
- **Репозиторий GitHub:** `https://github.com/limslava/calculator`
- **Amvera Console:** `https://cloud.amvera.ru`

---
## 10. Заключение

Система расчета логистических ставок представляет собой полнофункциональное приложение, готовое к использованию в production. Оно обеспечивает:

- **Гибкость** – поддержка различных типов перевозок (море, ЖД, комплексные)
- **Надежность** – использование PostgreSQL, JWT аутентификация, резервное копирование
- **Масштабируемость** – модульная архитектура, Docker-контейнеризация, CI/CD
- **Безопасность** – Content Security Policy, защита от XSS, шифрование паролей

Система успешно развернута на Amvera и может быть легко адаптирована для других облачных платформ. Дальнейшее развитие должно быть направлено на улучшение тестирования, типизации и пользовательского опыта.

---

**Дата последнего обновления документации:** 2026-01-13
**Версия системы:** 2.4.0 (Исправление модального окна маржинальности)
**Автор документации:** Roo (AI Assistant)
**Исходные файлы документации:** `README.md`, `PROJECT_STRUCTURE.md`, `DATABASE_SETUP.md`, `AMVERA_DEPLOYMENT.md`, `AMVERA_POSTGRESQL_GUIDE.md`, `docs/`, `IMPROVEMENT_SPEC.md`, `IMPROVEMENT_STATUS_REPORT.md`